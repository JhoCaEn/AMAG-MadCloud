specVersion: "3.0"
type: application
metadata:
  name: backoffice-partners

builder:
  resources:
    excludes:
      - "/test/**"
      - "/localService/**"
    
  customTasks:
    - name: ui5-task-zipper
      afterTask: generateCachebusterInfo
      configuration:
        archiveName: backoffice-partners