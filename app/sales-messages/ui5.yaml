specVersion: "3.0"
type: application
metadata:
  name: sales-messages

builder:
  resources:
    excludes:
      - "/test/**"
      - "/localService/**"
    
  customTasks:
    - name: ui5-task-zipper
      afterTask: generateCachebusterInfo
      configuration:
        archiveName: sales-messages